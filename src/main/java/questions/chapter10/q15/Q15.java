package questions.chapter10.q15;

import java.util.List;
import java.util.Map;
import java.util.stream.Stream;

public class Q15 {

    /*
15. Given the generic type String, the partitioningBy() collector creates a
Map<Boolean, List<String>> when passed to collect() by default. When a downstream
collector is passed to partitioningBy(), which return types can be created?
(Choose all that apply.)
A. Map<boolean, List<String>>
++B. Map<Boolean, List<String>>
C. Map<Boolean, Map<String>>
++D. Map<Boolean, Set<String>>
E. Map<Long, TreeSet<String>>
F. None of the above

     */

    public static void main(String[] args) {

        //Map<Boolean, List<String>>
        //Map<Boolean, Set<String>>
    }
}
