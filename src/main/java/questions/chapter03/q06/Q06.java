package questions.chapter03.q06;

public class Q06 {
    /*
    6. Which statements about decision structures are true? (Choose all that apply.)
A. A for-eachloop can be executed on any Collections Framework object. -->java.lang paketinde iterable interface ini implemente edenler yapabilir sadece
B. The body of a while loop is guaranteed to be executed at least once.
++C. The conditional expression of a for loop is evaluated before the first execution of theloop body.
++D. A switch expression that takes a String and assigns the result to a variable requires adefault branch.
++E. The body of a do/while loop is guaranteed to be executed at least once.
F. An if statement can have multiple corresponding else statements.
     */
}
